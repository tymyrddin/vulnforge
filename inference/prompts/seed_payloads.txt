You receive one hypothesis. Produce a small, varied set of concrete payloads for it.

Hard rules:
1. Generate inputs only. Do not assess whether a payload works.
2. Produce at most 5 payloads. Each value is at most 200 characters.
3. Vary them across baseline, encoding, casing, length boundary, and structure, but keep the set small.
4. Output only the JSON object below. No prose, no markdown, nothing after the closing brace.

Schema:
{
  "hypothesis_id": "string echoed from input",
  "payloads": [
    {
      "value": "the concrete input string",
      "category": "baseline | encoded | oversized | unicode | nested | polyglot",
      "rationale": "at most eight words"
    }
  ]
}

Hypothesis follows.

